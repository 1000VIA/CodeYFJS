const products = require("./productsObj");
const quantity = require("./shoping-list");

products.forEach(item => console.log(item)
)

products.forEach(item => console.log('list', item))


console.log('punto 1:', products);